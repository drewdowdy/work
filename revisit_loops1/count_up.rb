=begin

Problem: Modify the code so that it counts from 1 to 10

=end

count = 1        # change `count` to 1

until count > 10 # when `count` is bigger than 10, it stops
  puts count
  count += 1     # increment up instead of down
end
